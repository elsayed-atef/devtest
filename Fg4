# Function to Log Messages with Timestamp and Log Level
function Log-Message {
    param (
        [string]$Level,
        [string]$Message
    )

    $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
    $logEntry = "$timestamp [$Level] $Message"
    Write-Output $logEntry
    Add-Content -Path $LogFilePath -Value $logEntry
}

# Function to Check if Folder Name Matches Exclude Patterns
function Is-Excluded {
    param (
        [string]$folderName,
        [array]$patterns
    )

    foreach ($pattern in $patterns) {
        if ($folderName -match [regex]::Escape($pattern)) {
            Log-Message -Level "Debug" -Message "Folder '$folderName' matches exclude pattern '$pattern'."
            return $true
        }
    }
    return $false
}

# Function to Check if Current Time is Within Peak Hours
function Is-In-PeakHours {
    param (
        [datetime]$currentTime,
        [array]$timeRanges
    )

    foreach ($range in $timeRanges) {
        $startTime = [datetime]::ParseExact($range.Start, "HH:mm", $null)
        $endTime = [datetime]::ParseExact($range.End, "HH:mm", $null)

        # Adjust endTime for ranges that cross midnight
        if ($endTime.TimeOfDay -lt $startTime.TimeOfDay) {
            $endTime = $endTime.AddDays(1)
        }

        if ($currentTime.TimeOfDay -ge $startTime.TimeOfDay -and $currentTime.TimeOfDay -le $endTime.TimeOfDay) {
            Log-Message -Level "Debug" -Message "Current time $($currentTime.ToString('HH:mm')) is within peak hours range: Start $($startTime.ToString('HH:mm')), End $($endTime.ToString('HH:mm'))."
            return $true
        }
    }
    return $false
}

# Function to Perform Housekeeping Tasks
function Perform-Housekeeping {
    param (
        [xml]$config,
        [bool]$DryRun
    )

    foreach ($folder in $config.HousekeepingConfig.Paths.Path) {
        $path = $folder.Location
        $retentionDays = [int]$folder.RetentionDays
        $includeSubfolders = [bool]$folder.IncludeSubfolders
        $archiveEnabled = [bool]$folder.Archive.Enabled -eq 'true'
        $archivePath = $folder.Archive.Path
        $excludePatterns = $folder.ExcludePatterns.Pattern | ForEach-Object { $_ }
        $deleteIfEmpty = [bool]$folder.DeleteIfEmpty

        Log-Message -Level "Info" -Message "Processing folder: $path"
        Log-Message -Level "Debug" -Message "Retention Days: $retentionDays"
        Log-Message -Level "Debug" -Message "Include Subfolders: $includeSubfolders"
        Log-Message -Level "Debug" -Message "Archive Enabled: $archiveEnabled"
        Log-Message -Level "Debug" -Message "Archive Path: $archivePath"
        Log-Message -Level "Debug" -Message "Exclude Patterns: $($excludePatterns -join ', ')"
        Log-Message -Level "Debug" -Message "Delete If Empty: $deleteIfEmpty"

        # Retrieve all directories first, and then files if subfolders are included
        $directories = Get-ChildItem -Path $path -Directory -Recurse:$includeSubfolders
        $items = $directories | ForEach-Object { Get-ChildItem -Path $_.FullName -File }

        foreach ($item in $items) {
            # Check if the parent folder of the item should be excluded
            $parentFolder = Split-Path -Path $item.FullName -Parent
            $parentFolderName = [System.IO.Path]::GetFileName($parentFolder)

            if (Is-Excluded -folderName $parentFolderName -patterns $excludePatterns) {
                Log-Message -Level "Info" -Message "Skipping excluded folder: $parentFolder"
                continue
            }

            $itemAge = (Get-Date) - $item.LastWriteTime
            $itemPath = $item.FullName

            if ($itemAge.Days -gt $retentionDays) {
                if ($DryRun) {
                    Log-Message -Level "Info" -Message "Dry Run: Would delete $itemPath"
                } else {
                    Log-Message -Level "Info" -Message "Deleting $itemPath"
                    Remove-Item -Path $itemPath -Force
                }

                if ($archiveEnabled) {
                    if ($DryRun) {
                        Log-Message -Level "Info" -Message "Dry Run: Would archive $itemPath to $archivePath"
                    } else {
                        $archiveFile = Join-Path -Path $archivePath -ChildPath $item.Name
                        Log-Message -Level "Info" -Message "Archiving $itemPath to $archiveFile"
                        Move-Item -Path $itemPath -Destination $archiveFile
                    }
                }
            }
        }

        # Check if the folder is empty and delete if necessary
        if ($deleteIfEmpty -and (Get-ChildItem -Path $path).Count -eq 0) {
            if ($DryRun) {
                Log-Message -Level "Info" -Message "Dry Run: Would delete empty folder $path"
            } else {
                Log-Message -Level "Info" -Message "Deleting empty folder $path"
                Remove-Item -Path $path -Recurse
            }
        }
    }
}

# Main Loop
while ($true) {
    # Load XML Configuration
    $config = [xml](Get-Content "HousekeepingConfig.xml")

    # Extract global settings from XML
    $LogFilePath = $config.HousekeepingConfig.Settings.LogFilePath
    $MaxLogSizeMB = [int]$config.HousekeepingConfig.Settings.MaxLogSizeMB
    $cleanupIntervalHours = [int]$config.HousekeepingConfig.Settings.CleanupIntervalHours
    $DryRun = [bool]($config.HousekeepingConfig.Settings.DryRun -eq 'true')

    # Check if Peak Hours are Enabled and If the Script Needs to Sleep Until Peak Hours End
    $currentTime = Get-Date
    $peakHoursEnabled = [bool]($config.HousekeepingConfig.Settings.PeakHours.Enabled -eq 'true')
    $sleepUntilPeakHoursEnd = [bool]($config.HousekeepingConfig.Settings.PeakHours.SleepUntilEnd -eq 'true')
    $timeRanges = @($config.HousekeepingConfig.Settings.PeakHours.TimeRange.Range)

    Log-Message -Level "Debug" -Message "Peak Hours Enabled: $peakHoursEnabled"
    Log-Message -Level "Debug" -Message "Sleep Until Peak Hours End: $sleepUntilPeakHoursEnd"

    if ($peakHoursEnabled -and (Is-In-PeakHours -currentTime $currentTime -timeRanges $timeRanges)) {
        if ($sleepUntilPeakHoursEnd) {
            # Calculate the remaining sleep time until the end of the peak hours
            $endPeakHours = $timeRanges | Where-Object {
                $endTime = [datetime]::ParseExact($_.End, "HH:mm", $null)
                $endTime.TimeOfDay -gt $currentTime.TimeOfDay
            } | Sort-Object Start | Select-Object -First 1

            if ($endPeakHours) {
                $endTimePeak = [datetime]::ParseExact($endPeakHours.End, "HH:mm", $null)
                $sleepTime = ($endTimePeak.TimeOfDay - $currentTime.TimeOfDay).TotalMinutes
                Log-Message -Level "Info" -Message "Sleeping until peak hours end. Sleeping for $sleepTime minutes."
                Start-Sleep -Minutes $sleepTime
                continue
            }
        }
    }

    # Perform Housekeeping
    Log-Message -Level "Info" -Message "Starting housekeeping tasks..."
    Perform-Housekeeping -config $config -DryRun $DryRun
    Log-Message -Level "Info" -Message "Completed housekeeping tasks."

    # Log rotation check
    if ((Get-Item $LogFilePath).Length -gt ($MaxLogSizeMB * 1MB)) {
        Rename-Item -Path $LogFilePath -NewName "$LogFilePath.$(Get-Date -Format 'yyyyMMddHHmmss').log"
        Log-Message -Level "Info" -Message "Log file rotated."
    }

    # Sleep for configured interval
    Log-Message -Level "Info" -Message "Sleeping for $cleanupIntervalHours hours."
    Start-Sleep -Seconds ($cleanupIntervalHours * 3600)
}
